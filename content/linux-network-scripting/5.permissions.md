---
title: "Utilisateurs et Permissions"
description: "Gérer les utilisateurs et les permissions de fichiers sous Linux (chmod, chown, chgrp)."
---

## Utilisateurs et Permissions

### chmod - changer les bits de mode de fichier

```bash
chmod <permissions> <file>
```

Vous pouvez utiliser la notation symbolique pour changer les permissions. La syntaxe est :

| Symbole | Signification |
| :--- | :--- |
| `u` | utilisateur (propriétaire) |
| `g` | groupe |
| `o` | autres |
| `a` | tous (utilisateur + groupe + autres) |
| `+` | ajouter une permission |
| `-` | retirer une permission |
| `=` | définir la permission exactement |
| `r` | lecture |
| `w` | écriture |
| `x` | exécution |

**Exemples :**
- `chmod u=rw,go=r my_file.txt` : le propriétaire peut lire/écrire, le groupe peut lire, les autres peuvent lire.
- `chmod u=rwx,go=rx my_script.sh` : le propriétaire peut lire/écrire/exécuter, le groupe peut lire/exécuter, les autres peuvent lire/exécuter.
- `chmod u+x my_script.sh` : ajouter la permission d'exécution pour le propriétaire.
- `chmod go-w my_file.txt` : retirer la permission d'écriture pour le groupe et les autres.

### chown - changer le propriétaire et le groupe du fichier

```bash
chown <owner>:<group> <file>
```

**Exemple :**
```bash
chown user:group my_file.txt # changer le propriétaire en user et le groupe en group
```

### chgrp - changer la propriété du groupe

```bash
chgrp <group> <file>
```

**Exemple :**
```bash
chgrp group my_file.txt # changer le groupe en group
```

### Exercice Pratique

::tabs
:::tabs-item{label="Classique" icon="i-heroicons-list-bullet"}
1. Créez un fichier nommé `test_file.txt` dans votre répertoire actuel.
2. Vérifiez les permissions de ce fichier en utilisant `ls -l`.
3. Changez les permissions de `test_file.txt` pour que le propriétaire puisse lire et écrire, et que le groupe et les autres ne puissent que lire. Utilisez `chmod u=rw,go=r test_file.txt`.
4. Vérifiez à nouveau les permissions en utilisant `ls -l` pour vérifier le changement.
5. Quelles sont les permissions affichées pour `test_file.txt` après les avoir changées ?
:::

:::tabs-item{label="Guidé" icon="i-heroicons-numbered-list"}
    ::steps
    1. **Création du fichier**
    Créez un fichier nommé `test_file.txt` dans votre répertoire actuel.
    ```bash
    touch test_file.txt
    ```

    2. **Vérification initiale**
    Vérifiez les permissions de ce fichier en utilisant `ls -l`.
    ```bash
    ls -l test_file.txt
    ```

    3. **Modification des permissions**
    Changez les permissions de `test_file.txt` pour que le propriétaire puisse lire et écrire, et que le groupe et les autres ne puissent que lire.
    ```bash
    chmod u=rw,go=r test_file.txt
    ```

    4. **Vérification finale**
    Vérifiez à nouveau les permissions en utilisant `ls -l` pour vérifier le changement.
    ```bash
    ls -l test_file.txt
    ```

    5. **Analyse**
    Quelles sont les permissions affichées pour `test_file.txt` après les avoir changées ?
    *Réponse attendue : `-rw-r--r--`*
    ::
:::
